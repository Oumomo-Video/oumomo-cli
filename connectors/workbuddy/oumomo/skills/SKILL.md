---
name: oumomo-workbuddy
description: Use the public Oumomo CLI for deterministic video workflow commands.
version: "0.1.0"
---

Use only documented Oumomo commands. Preserve returned task IDs and poll the
public task status command. Never invent URLs, credentials, task IDs, or local
file paths. Never expose tokens or cookies.
