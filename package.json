{
  "name": "oumomo-agent",
  "version": "0.1.2",
  "private": false,
  "description": "Thin CLI client for published Oumomo skills with browser login, image upload, tool discovery, and allowlisted tool calls.",
  "type": "module",
  "bin": {
    "oumomo-agent": "dist/bin.js"
  },
  "main": "./dist/bin.js",
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "os": [
    "darwin",
    "linux",
    "win32"
  ],
  "cpu": [
    "x64",
    "arm64",
    "ia32"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/make-bin-executable.mjs",
    "test": "node --import tsx --test tests/*.test.ts",
    "prepare": "npm run build",
    "test:cli": "node --import tsx --test tests/*.test.ts",
    "prepack": "npm run build && npm test",
    "verify:size": "node scripts/verify-pack-size.mjs"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
